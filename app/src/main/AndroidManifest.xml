<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══ Permissions for Call Family + Help ═══ -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- ═══ NEW: Permissions for Memories + Medicine ═══ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ═══ Safety Monitoring Service Permissions ═══ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- ═══ Prescription Vault: Camera Capture ═══ -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Specific app queries for Genie + WhatsApp + Pharmacies -->
    <queries>
        <!-- WhatsApp for video call / messaging -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        
        <!-- Google Maps for location / hospital search -->
        <package android:name="com.google.android.apps.maps" />
        
        <!-- Food & delivery -->
        <package android:name="in.swiggy.android" />
        <package android:name="com.application.zomato" />
        
        <!-- Medicine Apps (Deep Linking) - Correct Play Store package names -->
        <package android:name="com.tatahealth.consumer" />  <!-- Tata 1mg -->
        <package android:name="in.apollo.android" />        <!-- Apollo Pharmacy -->
        <package android:name="com.healthkart.onemg" />     <!-- Tata 1mg (legacy) -->
        <package android:name="com.netmeds.app" />          <!-- Netmeds -->
        <package android:name="com.pharmeasy.app" />        <!-- PharmEasy -->
        <package android:name="com.apollo.pharmacy" />      <!-- Apollo (legacy) -->
        <package android:name="com.practo.fabric" />
        
        <!-- E-commerce -->
        <package android:name="in.amazon.mShop.android.shopping" />
        <package android:name="com.flipkart.android" />

        <!-- Intents -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.CALL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthPro">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HealthPro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Genie AccessibilityService -->
        <service
            android:name=".genie.GenieAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
        
        <!-- ═══ Safety Monitoring Foreground Service ═══ -->
        <service
            android:name=".safety.SafetyMonitoringService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- ═══ Mood Check-In Boot Receiver ═══ -->
        <receiver
            android:name=".mood.MoodBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ═══ Mood Check-In Alarm Receiver ═══ -->
        <receiver
            android:name=".mood.MoodAlarmReceiver"
            android:exported="false" />

        <!-- Medicine Reminder Worker (WorkManager handles this automatically, but explict config if needed) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- ═══ Prescription Vault: FileProvider for Camera ═══ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
